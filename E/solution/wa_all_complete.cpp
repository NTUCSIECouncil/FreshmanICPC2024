#include <bits/stdc++.h>
using namespace std;
typedef pair<int, int> pii;

int main() {
  int n;
  cin >> n;
  // vector<pii> task;
  for (int i = 0; i < n; i++) {
    int p, l;
    cin >> p >> l;
    // task.push_back({p, l});
  }
  cout << n << '\n';
  return 0;
}
